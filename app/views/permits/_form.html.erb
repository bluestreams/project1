<%= form_for(@permit) do |f| %>
  <% if @permit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permit.errors.count, "error") %> prohibited this permit from being saved:</h2>

      <ul>
      <% @permit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :permit_number %><br>
    <%= f.text_field :permit_number %>
  </div>
  <div class="field">
    <%= f.label :issued_date %><br>
    <%= f.date_select :issued_date %>
  </div>
  <div class="field">
    <%= f.label :expiry_date %><br>
    <%= f.date_select :expiry_date %>
  </div>
  <div class="field">
    <%= f.label :permitowner_id %><br>
    <%= f.number_field :permitowner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
